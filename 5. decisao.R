# Instrução IF
x <- 30L

if(is.integer(x)){
  print("X é um inteiro")
}

# IF... ELSE
x <- c("existe", "uma", "verdade")

if("foo" %in% x) {
  print ("foo foi encontrado")
} else {
  print ("foo não foi encontrado")
}

